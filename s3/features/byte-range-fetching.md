# Byte-Range Fetching

Byte-range fetching lets a client retrieve only part of an object, which is especially useful for media streaming and large files.

## Why it helps
- Reduces bandwidth usage
- Improves responsiveness for partial downloads
- Supports resumable and segmented transfer workflows
