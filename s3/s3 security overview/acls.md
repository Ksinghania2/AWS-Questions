# ACLs

Access Control Lists (ACLs) are a legacy access control mechanism in S3.

## Key point
AWS now recommends IAM policies and bucket policies for most use cases, but ACLs still appear in older configurations and historical discussions.
